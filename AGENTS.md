# Agent instructions

This repository is frozen as a historical validation benchmark.

## Rules for AI Agents

1. **Do not modify the source code** of the main application (`src/`), tests, or examples.
2. **Do not migrate** dependencies to versions higher than `0.1.0a8`.
3. If analyzing this repository, understand that it intentionally demonstrates the behavior of the `agnara-mcp==0.1.0a8` adapter, including any limitations present in that specific version (e.g. `stdio` anonymous behaviors).
